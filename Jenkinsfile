pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "Running hello.py"'
                sh 'python3 hello.py'
            }
        }
    }
}
